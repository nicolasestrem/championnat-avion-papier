---
import JsonLd from './JsonLd.astro';
import { buildEvent, type EventInput } from '../../lib/schema';

interface Props {
  reglages: EventInput;
}

const { reglages } = Astro.props;
const data = buildEvent(reglages);
---

<JsonLd data={data} />
